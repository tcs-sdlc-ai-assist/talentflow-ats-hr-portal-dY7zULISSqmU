# TalentFlow ATS - Environment Configuration
# Copy this file to .env and fill in your values

# Secret key for signing sessions and tokens (generate a strong random string)
# Example: openssl rand -hex 32
SECRET_KEY=change-me-to-a-random-secret-key

# Database URL for SQLAlchemy async engine
# For SQLite (development): sqlite+aiosqlite:///./talentflow.db
# For PostgreSQL (production): postgresql+asyncpg://user:password@localhost:5432/talentflow
DATABASE_URL=sqlite+aiosqlite:///./talentflow.db

# Default admin account created on first startup
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=changeme123

# Session maximum age in seconds (default: 86400 = 24 hours)
SESSION_MAX_AGE=86400

# Environment: dev or production
# Controls debug mode, CORS settings, and logging verbosity
ENVIRONMENT=dev